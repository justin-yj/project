import 'package:flutter/material.dart';

class Pages extends StatelessWidget{
    @override
    Widget build(BuildContext context){
        return Scaffold(
              appBar: AppBar(title:new Text("Pages")),
              body: Center(
                child: Text("Pages"),
              ),
        );
    }
}